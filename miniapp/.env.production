# ============================================================
# 生产环境 API 地址 —— 部署前必须填写
# ------------------------------------------------------------
# 微信小程序硬性要求：
#   1. 必须 HTTPS（http 只允许在开发者工具里临时测试）
#   2. 域名必须在微信小程序后台的「服务器域名」白名单里登记
#   3. nginx 把 /prod-api/ 反代到 api:8088，所以 base 写到域名根即可
#
# 部署后改为类似：
#   VITE_API_BASE_URL=https://api.your-domain.com/prod-api
#
# 在没有正式域名前，请勿运行 npm run build:mp-weixin，
# 否则打出来的小程序包指向不可用的地址。
# ============================================================
VITE_API_BASE_URL=
